- Расскажите о себе
- Почему решили уволиться, почему ищете новую работу 
- Что вы ищете сейчас
- Чем вы занимались на работе
- Каким достижением на прошлой работе можете гордиться
- Назовите 3 положительных качества, 3 отрицательных

- ЗАДАЧИ

- Что изучаете в последнее время
- Есть ли предложения от других компаний
- На какую ЗП рассчитываете


```go
Что будет в результате выполнения?
===========================================================

mutate := func(a []int) {
  a[0] = 0
  a = append(a, 1)
  fmt.Println(a)
}
a := []int{1, 2, 3, 4}
mutate(a)
fmt.Println(a)
```

---

```go
Исправить функцию, чтобы она работала. Сигнатуру менять нельзя
===========================================================

func printNumber(ptrToNumber interface{}) {
        if ptrToNumber != nil {
                fmt.Println(*ptrToNumber.(*int))
        } else {
                fmt.Println("nil")
        }
}

func main() {
        v := 10
        printNumber(&v)
        var pv *int
        printNumber(pv)
        pv = &v
        printNumber(pv)
}
```

---

```go
Что выведется и как исправить?
===========================================================

func main() {
  var counter int
  for i := 0; i < 1000; i++ {
    go func() {
      counter++
    }()
  }
  fmt.Println(counter)
}
```

---

```go
Задача 6
1. Реализовать кеш. Для простоты считаем, что у нас бесконечная память и нам не нужно задумываться об удалении ключей из него.
	1. Почему использовал RWMutex, а не Mutex?
2. Теперь представим что память не бесконечная. С какими проблемами столкнемся и как их решить?
	2. Какие есть алгоритмы выселения?
3. Реализуй LRU
===========================================================

// In-memory cache
// Нужно написать простую библиотеку in-memory cache.
// Реализация должна удовлетворять интерфейсу:

type Cache interface {
    Set(k, v string)
    Get(k string) (v string, ok bool)
}
```

---

``` 
Ревью кода
github.com/nosuchpersonn/some_app
```

---

```sql
https://sqltest.online/ru/question/db-theory/what-is-normalization

https://sqltest.online/ru/question/db-theory/index-benefit

https://sqltest.online/ru/question/sql-basics/find-addresses-without-postal-code

https://sqltest.online/ru/question/sql-basics/find-employees-by-hire-date
```

---

```
Вот вопрос, который хорошо проверит знание **Kafka, NoSQL БД и System Design** у кандидата:

**"Вы разрабатываете систему аналитики пользовательских действий для высоконагруженного веб-приложения. Данные поступают в реальном времени и должны обрабатываться с минимальной задержкой. Как бы вы спроектировали архитектуру backend-части с использованием Kafka и NoSQL БД? Какие проблемы могут возникнуть, и как их решить?"**

Этот вопрос проверяет:

- **Kafka** – как кандидат организует потоки данных, партиционирование, обработку сообщений.
- **NoSQL БД** – какую выберет (например, Cassandra, MongoDB, DynamoDB), как организует хранение и индексацию.
- **System Design** – масштабируемость, отказоустойчивость, CAP-теорему, event-driven архитектуру.

Ты можешь углубиться и уточнить детали:

- Какой ключ для партиционирования Kafka выберешь и почему?
- Как организуешь deduplication событий?
- Какой тип NoSQL БД предпочтительнее в данном случае?
- Как обеспечишь exactly-once обработку сообщений?

Если хочешь проверить знание **Golang**, можешь спросить про реализацию Kafka-консьюмера и взаимодействие с NoSQL в Go.
```
